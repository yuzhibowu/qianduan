# 前端→Keynote UI 标准

本文档记录当前项目已经确认的界面规范。它适用于新增 OriginKit 案例、增加参数、调整导出能力和修复界面问题。界面工作开始前应先阅读本文档；如果新需求明确改变了既有规范，应同步更新本文档，不能只在某个组件里做一次性样式。

## 一、设计目标

本工具是面向 Keynote 的前端动效转换器，不是网页编辑器，也不复刻 OriginKit 官网的编辑器外观。界面应像简洁、克制的专业设计工具：主要精力放在预览、参数调整和导出，减少装饰性边框、阴影、圆角和无意义说明。相同功能必须使用相同视觉语言，不同技术类型的案例则按真实能力显示不同参数与导出选项。

所有可见文案优先使用中文，名称本身属于组件或格式专名时可保留英文，例如 Light Bloom、MOV、PNG、USDZ。左上角产品名固定为“前端→Keynote”，右侧显示当天版本号，格式为 `YYMMDDX序号`，例如 `260910X9`。明暗模式按钮只显示太阳或月亮图标，不添加灰色方框。

## 二、整体布局

页面采用左右两栏结构。左侧是主要预览区，右侧是固定宽度的参数面板。桌面宽度下页面外边距和左右栏间距保持稳定，预览区获得剩余空间，右侧面板不因组件切换改变宽度。右侧顶部放组件选择器和“饼饼SHOW”标识，下面才是可滚动的参数内容。

右侧滚动条必须细而克制，宽度为 3px，滑块使用纯黑色，轨道透明，不额外绘制轨道分割线。滚动只发生在右侧参数区；鼠标位于左侧预览区域时，触控板双指上下滑动只控制预览对象缩放，不能带动整个页面上下滚动。

## 三、预览区

预览区外围随界面明暗模式显示纯白或深色背景，不显示额外的深色框。真正的输出画布位于中央，透明状态使用棋盘格，并绘制当前输出比例的约束框。16:9 和 1:1 都必须明确显示相应边界，不能只给 1:1 画框。

预览内容应在输出画布中真正居中，缩放只改变预览对象大小，不改变导出尺寸和宽高比。预览必须运行与导出相同的原始组件、参数和时间模型，不能用静态缩略图或近似动画替代。组件菜单的悬浮缩略框也必须播放真实动画，并出现在鼠标附近，但不能遮挡当前菜单项。

背景选项影响的是整个输出画布，而不是组件内部的一小块矩形。选择黑色或白色后，完整棋盘格范围都应切换为相应颜色；选择透明时才显示棋盘格。组件内部自带的背景参数与输出画布背景是两类参数，不能混为一项。

## 四、组件选择器与分类

组件选择器使用项目自定义菜单，不能使用浏览器或系统默认下拉菜单。菜单只能从触发按钮向下展开，背景为纯黑矩形，不添加圆角。未选项目使用淡灰色文字，当前选中项目使用白色文字，悬浮时仍保持纯黑背景。

组件按真实动画类型分组。当前至少区分真实 3D 对象和网页视觉效果；后续可继续增加文字动画、背景动画、滚动动画、鼠标悬浮动画等类别。类别之间使用一条低对比度分割线，并为分割线保留合理上下间距，不能紧贴前后菜单项。分类只负责组织，不能把不同技术类型强行变成同一种渲染方案。

## 五、右侧信息顺序

普通组件的右侧面板从上到下依次为：输出画布背景、组件颜色、组件专属参数、绝对时间与循环、导出参数、MOV 与 PNG 导出、苹果原生 3D 格式。组件不具备某项能力时，应隐藏无意义的参数或将对应导出按钮置灰，并给出简短原因。

输出画布背景区域在普通案例中位于最上方，包含“透明、黑色、白色、其它”四个选项，默认选择透明。“其它”只显示这两个汉字，不默认展示蓝色色块；用户点击后才进入颜色选择。Light Bloom 这类全画布背景案例不显示这组通用背景选项，因为它们已有直接参与效果的组件背景色参数。

“组件参数”和“时间验证”这类没有信息价值的总标题不显示。参数应直接按照“颜色、样式、方向、光源、光束、质感”等真实字段组织。官方存在的有效参数必须完整列出，不能因为面板较长而省略。

## 六、分割线与间距

右侧面板所有主要分割线必须遵循同一套垂直节奏。标准窗口中，分割线上方内容到线、线到下方内容的有效留白均以 18px 为标准；高度不超过 760px 的紧凑窗口统一缩减为 10px。不得在单个标题或控件上另写 14px、15px、19px 等近似数值来模拟相同间距。

分割线必须放在区块边界，而不是塞进第一个控件内部。前一个区块提供线前留白，后一个区块提供线后留白，二者不能叠加成双倍空隙。导出区域不得使用 `margin-top: auto` 自动吞掉剩余高度，否则参数少的案例会出现巨大空白，而参数多的案例不会，造成组件之间不一致。

组件内部的二级分组标题也使用同一个区块间距变量。标题上方需要分割线时，线前和线后都使用统一间距；标题下方到第一项控件只保留字段自身的正常间距。纯装饰性分割线不增加，只有用于表达参数层级、时间区、导出区或格式类别时才显示。

当前样式以 CSS 变量 `--section-gap` 作为唯一基准：标准值为 18px，紧凑模式为 10px。新增区块必须复用该变量，不得另建一套近似间距。

## 七、字段与控件

同级字段标题使用一致的字号、字重和颜色。“颜色”二字必须与“样式、方向、光源”等同类字段完全一致，不能单独加粗或放大。字段标签与控件之间保持稳定距离，文字输入字段不能紧贴上一组颜色控件。

按钮采用直角矩形和细边框。选中状态使用深色实底和浅色文字，未选状态使用页面底色、深色文字和浅灰边框。选项尺寸应在同一行等宽；16:9、1:1、720P、1080P、2K、4K 等关键选择必须排列清楚，不能依赖原生下拉框。

连续参数使用滑杆，必须显示当前数值。尺寸比例滑杆从 1:3 连续变化到 3:1，在 1:3、1:2、9:16、3:4、1:1、4:3、16:9、2:1、3:1 等关键比例处提供刻度和吸附，但不能把连续调节改成固定档位。刻度文字不能相互重叠，窄区间可错层或采用更短的显示方式。

Neon Border 导入 PNG 后，边框粗细和发光长度应按透明画布中实际可见图形的细长程度自动适配一次，横向与竖向的同等细长比应得到相同建议值。自动适配不得在用户手动调整后持续覆盖数值；点击字段名恢复当前插图比例的建议值，去掉插图后恢复 Neon 原始默认值。

边框类案例的图片分为“背景插图”和“PNG 插图”两类。背景插图始终只有一张，其实际 Alpha 外轮廓决定发光边缘，不能用统一矩形或统一圆角代替左右不对称的真实边界。PNG 插图可叠加多张，只参与画面合成而不改变发光路径；后加图层可在预览区直接拖动位置，选中后通过右下角锚点等比缩放，编辑锚点不进入导出，位置和缩放结果必须同步进入逐帧导出。当尚未有背景插图时，拖入或粘贴的第一张 PNG 作为背景插图；后续 PNG 按普通图层新增。

从背景插图的 Alpha 生成发光边缘时，禁止把阈值后的像素边界描成锯齿位图线，也禁止用可能越出原图的全局三次贝塞尔曲线重新拟合轮廓。发光条外缘必须直接沿用原 PNG 的连续 Alpha；内缘必须由同一 Alpha 边界的欧氏距离场生成等距内缩线，使内外圆角共用轮廓关系并保持恒定法向厚度。不得用矩形取样核的形态收缩、另一条独立拟合曲线或单独的圆角参数代替等距内缩，因为这些方法会造成拐角与直边粗细不同。距离场允许输出高分辨率 Alpha 遮罩，但只能表达源轮廓到等距内缘之间的区域，并须保留连续 Alpha 抗锯齿，不能重新栅格化一条近似边界。发光核心和各层光晕必须共享这套距离场与源 Alpha，不允许出现尖线伸出主体、像素折点形成的颗粒、内外圆角不一致、直边与圆角粗细不一致，或用统一圆角代替不对称轮廓。原组件的渐变、颜色、光晕层级和时间模型不得因轮廓适配而改变，预览与逐帧导出必须调用同一个遮罩生成器。

Neon Border 导入背景插图后，发光条的长度和位置必须以插图真实 Alpha 外轮廓的周长为坐标，而不是以画布中心角度或固定像素宽度为坐标。上下长边、左右短边和圆角段上的同一发光长度必须占用相同的路径长度，不得在细长素材的左右两侧视觉收窄；第二条光始终位于第一条光沿真实周长相隔 50% 的位置。中心角度只允许作为保留原有锥形渐变绘制方式的最终映射结果，不能再负责定义发光条长度。

## 八、动画曲线面板

网页案例需要开放动画时间曲线时，统一复用 Disc Split 已确认的动画曲线面板，不得临时改成一排预设按钮、系统原生下拉菜单、单独的数值滑杆或另一套一次性设计。面板顶部同一行左侧显示“动画曲线”，右侧放紧凑的自定义菜单；菜单触发器使用项目字段控件的直角细边框样式，展开菜单必须沿用组件选择器的视觉语言：纯黑矩形、无圆角，未选项为淡灰文字，当前项及悬浮项为白色文字。标准预设依次包含“匀速、缓入、缓出、缓入缓出、自定义”，案例确实需要不同命名或额外曲线时，必须由其真实参数能力决定，不能随意删减通用预设。

曲线画布固定为占满字段可用宽度、高 112px 的横向面板，背景必须与普通滑杆未完成部分的浅灰轨道使用同一个颜色变量。画布不显示外围边框、虚线框或井字网格。曲线的起点与终点应分别贴近左下角和右上角，只保留防止线帽与控制点被裁切所需的最小安全距离，不能在左右两侧留下明显空白，也不能为了贴边而把曲线强行画成直线。曲线本体使用项目统一的黄色；允许显示连接端点与控制点的低对比度虚线辅助线以及可拖拽控制点，但辅助线不得形成背景网格。

拖动任一控制点后，菜单状态自动切换为“自定义”；选择任一预设后，必须一次性恢复该预设的全部控制点。曲线数据必须进入案例的统一参数状态，并由正常预览、绝对时间预览、逐帧 MOV/PNG 导出以及案例真实支持的其他导出格式共同使用，禁止面板只改变示意图而不改变动画。动画速度与动画曲线属于同一个动画参数组，二者之间不增加分割线；曲线面板与下一个不同含义的参数组之间使用标准区块分割线，上下间距统一取 `--section-gap`，即标准窗口 18px、紧凑窗口 10px。

## 九、颜色控件

颜色字段由标签和一整块可点击色块组成。色块占满原输入框的可用宽度和高度，不再在色块外套一层输入框边框。色块内部直接显示大写十六进制颜色编码，例如 `#6B2BF5`，文字不添加阴影、描边或其他效果。

“主体颜色”和“高光颜色”的字段名与滑杆字段名遵循同一恢复规则：点击字段名应恢复当前案例在组件登记表中的默认颜色。这一交互只改变可点击语义，不得改变原有字号、字重、间距或色块尺寸。

色码文字颜色应根据色块明暗自动选择黑色或白色，保证可读性。纯白色块因为会与页面背景融为一体，必须显示一条浅灰色边框；其他颜色不额外加框。组件自身存在背景色时，背景色应放在该组件颜色字段的最上方，然后才是主体颜色和高光颜色。

## 十、绝对时间与导出参数

“绝对时间”区域上方必须有分割线，并与上下区域保持统一间距。循环预览与绝对时间属于同一区域，不再额外增加总标题。播放、暂停和回到开头属于预览操作，应保持清晰但不抢占参数区视觉重心。

当案例的“动画速度”会改变真实循环周期时，速度、绝对时间上限和导出参数中的“完整周期”必须由同一个周期公式联动；调整速度后，绝对时间滑到终点必须仍然恰好完成一整个周期，MOV 与 PNG 动图也必须按更新后的完整周期取样。禁止渲染器使用新速度而时间面板继续保留旧周期。

“导出参数”上方必须有分割线。内部依次显示画布比例、输出分辨率、宽高数值、帧率、完整周期和开始延迟。画布比例默认为“自适应”，它位于 1:1 之后：自适应不是只取素材原始宽高，而是在完整动画周期的所有唯一帧中，计算主体、边框、发光和阴影的视觉有效 Alpha 并集。高斯模糊会产生远处非零但已不可见的数值尾巴，不得仅以 Alpha 大于零作为边界；当前统一忽略低于约 3% 不透明度的尾巴，并在可见并集外保留 4px 安全边距。预设分辨率包含 720P、1080P、2K、4K，默认为 720P，默认帧率为 30 FPS。所有案例使用相同导出参数排布，只有真实能力不同的格式按钮可以变化。

“无损压缩 PNG 帧（MOV 与 PNG 动图）”紧挨“导出透明 MOV”按钮，并位于导出参数下方分割线之后，默认勾选。“保留透明 PNG 序列”选项不再显示。准备就绪文字和导出进度条平时隐藏，只在导出任务进行时出现。

“导出透明 MOV”和“导出 PNG 动图”是两项相互独立的任务。启动其中一种格式时，只允许对应按钮进入导出状态并显示该格式自己的进度、错误和取消操作；另一个格式的按钮在文字、颜色、可用状态上都不得发生变化。两种格式同时导出时，各自状态不得互相覆盖或共同取消。

MOV 与 PNG 动图之后用带文字的分割线进入“苹果原生3D格式”区域。这个名称不能写回“Keynote 原生 3D”。USDZ 不支持的网页效果将按钮置灰；支持 USDZ 的案例才显示可用按钮。

## 十一、Keynote 偏色抵消

“偏色抵消”只属于 USDZ 导出，必须放在“导出动画 USDZ”按钮上方，不能放到 MOV 或 PNG 区域。默认不勾选。未勾选时，“Keynote、无编辑、重新校准”等附加选项全部隐藏；勾选后才展开这些控件。未选按钮使用正常灰色未选状态，不能显示成深黑色选中状态。

## 十二、条件显示原则

条件显示以组件注册信息和真实能力为依据，不应到处硬编码单个组件名称。全画布背景类可以隐藏通用画布背景区；文字类显示文字内容和本机字体；交互类显示录制交互入口；真实 3D 类开放 USDZ；无法形成真实几何的网页视觉效果关闭 USDZ。

隐藏某一区域时，前后区块必须自然衔接，不能留下空白占位、重复分割线或双倍间距。切换组件后要检查顶部、组件参数末尾、绝对时间、导出参数和格式区域五个边界，确保参数数量变化不会破坏统一节奏。

## 十三、UI 修改验收清单

1. 至少切换一个参数较少和一个参数较多的案例，确认右侧分割线间距一致。
2. 检查普通组件与 Background 类组件，确认条件区域隐藏后没有空白或重复分割线。
3. 检查明暗模式，保证外围背景、文字、边框、色块和棋盘格均清晰。
4. 检查 16:9 与 1:1，确认预览约束框、居中和缩放行为正确。
5. 检查所有选中、未选中、禁用和条件展开状态，不能只检查默认页面。
6. 检查右侧滚动到最下方时，导出控件顺序和间距没有被自动撑开。
7. 完成 TypeScript 构建、自动化测试和 `git diff --check`；视觉观感仍由用户进行最终验收。
8. 对包含动画曲线面板的案例，检查预设切换、自定义控制点拖动、左右端点留白、浅灰背景、菜单样式及预览与导出联动。
