# 透明动画导出架构与接入规范

## 1. 文档目的

本文档用于在新的网页动画应用中复用一套高性能、透明底、所见即所得的动画导出方案。它覆盖 MOV（带 Alpha 的 ProRes 4444）与 PNG 动图（APNG），也规定了自适应裁切、绝对时间采样、进度显示、内存保护和验收方法。新案例接入时，应先判断动画的真实技术类型，再选择对应的抓帧路径；不能为了统一代码而把 Canvas、WebGL、SVG、DOM/CSS 或三维场景强行改写成同一种实现。

这套架构的目标不是单纯“把页面截图很多次”，而是建立一条确定性的渲染流水线：给定同一个绝对时间和同一组参数，预览与导出必须得到同一帧；导出器只负责组织时间、透明合成、裁切与编码，不得擅自改变原始动画的颜色、光效、几何形状或运动曲线。

## 2. 适用范围

本规范适用于参数化网页动画、文字效果、边框光效、加载动画、SVG 动画以及 Canvas/WebGL 动画。它尤其适合最终素材只需要“效果本身”、不需要预览背景的场景，例如把透明 MOV 或 APNG 放进 Keynote、剪辑软件或其他合成工具。

USDZ 与真正的三维场景不走本文的逐帧 MOV/APNG 编码管线。它们可以复用绝对时间、能力登记、校验和进度设计，但材质、纹理、色彩空间和 USDZ 动画采样必须由单独的三维导出模块处理。

## 3. 最高原则

### 3.1 原始效果只有一个真源

预览和导出必须调用同一套状态计算与渲染逻辑。禁止另写一套“看起来差不多”的导出动画，也禁止通过 CSS 近似、重新推导曲线或替换官方实现来取得导出帧。导出适配只能增加绝对时间入口、参数桥接、透明合成、裁切和编码，不得改变效果本身。

### 3.2 背景选择只用于预览

界面中的“透明、黑色、白色、其他”用于帮助用户检查不同底色上的效果，不代表导出底色。MOV 和 PNG 动图默认必须导出透明底。组件自身不可分割的内容，例如用户导入的 PNG、玻璃面板、文字底板或效果原本包含的画布背景，仍属于作品内容，不能被误删。

对于 Ripple、Light Bloom 等“整张背景就是效果本体”的案例，需要在能力登记中明确标记为固有背景组件。此时导出的背景不是预览底色，而是组件内容。

### 3.3 绝对时间驱动

导出器不得依赖 `requestAnimationFrame` 已经运行了多久，也不得依赖抓帧速度。每一帧必须通过绝对时间独立计算：

```ts
const uniqueFrameCount = Math.round((durationSeconds + delaySeconds) * fps)
const absoluteTime = Math.max(0, frameIndex / fps - delaySeconds)
```

只导出 `0 ... uniqueFrameCount - 1` 的唯一帧，不在结尾重复首帧。这样可以避免循环接缝处停顿一帧，也使不同性能的电脑导出相同结果。

### 3.4 先分类，再优化

性能优化必须建立在组件类型之上。Canvas/WebGL 应直接读取已有画布；SVG 应优先直接光栅化；DOM/CSS 才使用 DOM 截图作为兜底。不能因为当前导出器已经支持 DOM 截图，就让所有案例都走最慢、最不稳定的路径。

## 4. 组件能力登记

每个新案例都应在统一登记表中声明自己的导出能力，避免在导出器里不断增加散落的名称判断。

```ts
type FrameSourceKind =
  | "canvas"
  | "webgl"
  | "svg"
  | "dom"
  | "background"
  | "three-dimensional"

type BoundsStrategy =
  | "static-geometry"
  | "full-cycle-alpha-union"
  | "full-frame"

interface ExportCapability {
  id: string
  frameSource: FrameSourceKind
  boundsStrategy: BoundsStrategy
  intrinsicBackground: boolean
  supportsTransparentMov: boolean
  supportsApng: boolean
  captureSelector?: string
  captureFrame?: (absoluteTime: number) => Promise<HTMLCanvasElement>
}
```

登记表是扩展新案例的入口。新增同类型案例时，只要实现约定的绝对时间渲染和抓帧接口，就应自动获得透明导出、自适应裁切、进度、取消、编码与验证能力，而不是再次复制完整导出流程。

## 5. 渲染类型与抓帧路径

| 类型 | 首选抓帧方式 | 自适应策略 | 说明 |
| --- | --- | --- | --- |
| Canvas 2D | 直接将原画布绘制到导出画布 | 静态几何或全周期 Alpha 并集 | 不做 DOM 截图 |
| WebGL | 直接读取或合成 WebGL 画布 | 静态几何或全周期 Alpha 并集 | 必须保证导出时画布内容可读取 |
| SVG | 以指定时间更新 SVG 后直接光栅化 | SVG 几何或全周期 Alpha 并集 | 保留矢量轮廓质量 |
| DOM/CSS | 同源离屏页面加 DOM 截图兜底 | 通常为全周期 Alpha 并集 | 仅在没有更直接路径时使用 |
| 整幅背景效果 | 抓取完整效果画布 | 全画幅或明确的效果边界 | 背景属于内容本身 |
| 3D/USDZ | 独立三维导出器 | 三维包围盒 | 不进入 MOV/APNG 管线 |

对由多层组成的案例，抓帧函数应在同一个透明画布中按预览顺序合成。例如边框效果可依次绘制用户背景插图、官方 WebGL 光效、真实 Alpha 遮罩、模糊光晕和普通 PNG 图层。合成顺序、混合模式、透明度和裁切路径必须与预览保持一致。

## 6. 推荐的统一接口

```ts
interface ExportSettings {
  fps: number
  durationSeconds: number
  delaySeconds: number
  resolution: "720p" | "1080p" | "2k" | "4k"
  aspect: "16:9" | "1:1" | "adaptive"
  background: "transparent"
}

interface FrameContext {
  frameIndex: number
  frameCount: number
  absoluteTime: number
  outputWidth: number
  outputHeight: number
  crop?: { x: number; y: number; width: number; height: number }
}

interface FrameRenderer {
  prepare(settings: ExportSettings): Promise<void>
  render(context: FrameContext): Promise<HTMLCanvasElement>
  dispose(): Promise<void> | void
}
```

`prepare` 只做一次性的字体、图片、解码器、离屏页面和 GPU 资源准备；`render` 只计算指定时间的单帧；`dispose` 必须释放 iframe、对象 URL、临时画布、WebGL 资源和编码器文件。任何每帧都重复进行的资源加载，都应优先移入 `prepare`。

## 7. 同源离屏渲染页

浏览器导出建议创建同源离屏 iframe，例如 `?render=frame`。离屏页复用正式组件代码，但隐藏编辑器、控制面板、选中框、拖拽锚点和预览棋盘格。父页面通过受控接口传入案例 ID、参数和绝对时间，再取得透明画布。

离屏页必须与主页面使用相同构建产物和样式资源。不要用另一套简化 HTML 模板重建效果。字体、图片和动画资源应在开始抓帧前完成加载；否则首帧会出现空白、字体跳变或尺寸变化。

## 8. 透明合成规则

导出画布创建后应保持透明，不得先填充当前预览背景。所有图层都以真实 Alpha 合成：

```ts
ctx.clearRect(0, 0, width, height)
// 按正式预览的图层顺序 drawImage / 绘制路径
```

如果效果需要遮罩，遮罩应来自用户素材的 Alpha 或组件的真实几何轮廓，而不是通过“接近黑色”“接近白色”等颜色猜测。预览棋盘格、编辑区灰底、选择边框和辅助线永远不能进入导出结果。

导出后的 Alpha 必须实际验证，不能只看文件扩展名。至少要确认同一解码帧中同时存在透明像素、半透明像素和不透明像素。

## 9. 自适应裁切

### 9.1 定义

自适应不是裁到素材本体的矩形边界，而是裁到一个完整动画周期内所有可见内容触及区域的并集，再加入少量安全边距。阴影、模糊、辉光、位移、缩放和旋转都属于有效显示区域。

### 9.2 静态几何快速路径

当主体几何不随时间改变，只有颜色或光条沿固定轮廓运动时，可根据主体几何、真实 Alpha 边界、变换、边框包裹位置和最大辉光半径一次算出裁切框。这类案例不需要先扫描整个周期，能够显著缩短 0% 阶段。

### 9.3 全周期 Alpha 并集

如果导入的是动图 PNG、存在可移动/缩放的叠加图层、主体轮廓随时间变化，或者无法可靠证明几何静态，应使用两遍确定性导出：第一遍渲染所有帧并累计 Alpha 包围盒，第二遍按最终包围盒重新渲染并编码。

```ts
for (let i = 0; i < frameCount; i++) {
  const frame = await renderer.render(contextFor(i))
  union = unionAlphaBounds(union, frame, { threshold: 8 })
}

const crop = addPadding(union, 4)
```

建议 Alpha 阈值使用 `8/255` 左右，以排除不可见噪点；安全边距可从 4 像素起步，并按效果最大模糊半径修正。不能使用固定的大边距，否则导入 Keynote 后会出现明显的透明空白。

### 9.4 决策规则

能从参数和真实素材边界可靠推出整个周期的最大范围时，选择静态几何；范围随时间变化或无法证明时，选择全周期并集；组件本身就是全画幅背景时，选择全画幅。这个判断应写进能力登记，而不是由用户手动选择。

## 10. MOV 编码

透明 MOV 推荐使用 ffmpeg.wasm 最终编码为 ProRes 4444 XQ。每帧先通过 `canvas.toBlob("image/png")` 得到无损 PNG，直接写入编码器文件系统；不要在 JavaScript 中再解码、重压缩或长期保存全部帧。

```bash
-c:v prores_ks
-profile:v 5
-bits_per_mb 8000
-pix_fmt yuva444p10le
-alpha_bits 16
-vendor apl0
```

帧率必须来自导出设置，帧数必须与绝对时间公式一致。编码阶段只进行一次最终 ProRes 编码。PNG 帧和 MOV 是两条独立任务：导出 PNG 动图时，不应改变 MOV 按钮的状态；导出 MOV 时，也不应占用 PNG 动图按钮的状态。

## 11. PNG 动图编码

PNG 动图采用 APNG，使用完整帧或经过严格验证的差分帧策略。优先顺序是透明正确、无黑色频闪、循环无接缝，其次才是文件体积。若差分帧的 disposal/blend 处理不完全可靠，应使用完整帧。

勾选“无损压缩 PNG 帧”不应改变画面内容或 Alpha。编码过程中要逐帧释放临时 Blob、ImageBitmap 和 ArrayBuffer，避免高分辨率长动画触发浏览器内存保护。

## 12. 性能与内存

性能瓶颈通常不是动画计算，而是每帧 DOM 克隆、样式解析、图片序列化、PNG 编码、跨线程复制和长期保留帧数据。优化顺序应为：先移除不必要的 DOM 截图，再缓存静态资源，然后减少像素复制，最后才调整编码参数。

必须遵守以下规则：

- Canvas/WebGL/SVG 有直接抓帧路径时，不得退回整页 DOM 截图。
- 字体、PNG、SVG、遮罩和静态布局只准备一次。
- 标准尺寸导出逐帧生产、逐帧写入、逐帧释放，不在 JS 内存中保存全部解码帧。
- 自适应需要两遍时，第一遍只保留包围盒统计，不保存帧图像。
- 设置明确上限，例如单边不超过 8192 像素、帧数不超过 3600、总处理像素不超过 20 亿；超过后给出可理解的降级建议。
- ffmpeg 核心、工作线程和资源只加载一次并复用，任务结束后清理虚拟文件系统中的临时帧。

同类案例的直接抓帧架构曾把 720p、30 FPS、10 秒、300 帧的导出从约 10 分钟缩短到约 45 秒。这个数据只说明架构方向有效，不应成为所有设备上的固定性能承诺。

### 12.1 本机 FFmpeg 高速通道

本地运行的应用应在 MOV 导出前自动探测本机助手是否在线，并由助手检查系统 FFmpeg、`prores_ks` 和 Alpha ProRes 能力。能力合格时，网页仍使用同一个组件渲染器逐帧生成无损透明 PNG，但每帧生成后立即通过同源本机接口写入 FFmpeg 的 `image2pipe` 标准输入；无需加载浏览器版 ffmpeg.wasm，也无需把所有帧复制到 WebAssembly 虚拟文件系统。FFmpeg 完成后，助手以文件流提供下载，不能先把整部 MOV 再复制进 Node 和浏览器内存。

本机助手不可用、FFmpeg 缺失或能力检测不合格时，MOV 应自动回退到随网页发布的 ffmpeg.wasm，PNG 动图应自动回退到浏览器内已经验证的完整帧 APNG 编码器，用户的导出能力不能因此消失。MOV 与 PNG 动图仍是两项互不影响的任务。MOV 的本机通道使用 FFmpeg ProRes 4444；APNG 不采用 FFmpeg 默认的局部差分帧优化，而由本机助手直接复用各张无损 PNG 的压缩数据，逐帧写成覆盖完整画布的 APNG 控制块，以降低内存占用并规避部分播放器因差分帧处理不一致造成的黑色频闪。自动选择只替换编码执行位置，不改变抓帧器、绝对时间、帧数、分辨率、自适应边界、透明合成或质量参数。

普通线上网页受浏览器安全限制，不能直接执行用户电脑上的 `ffmpeg`。因此本机高速通道必须由可信的本地开发服务、桌面壳或专用助手提供，并且只开放固定的能力检测、开始任务、追加 PNG 帧、完成、下载和取消接口；禁止接受任意命令行参数或执行任意系统命令。

公开网站接入独立助手时，助手必须只监听回环地址，并对 `Origin` 做精确白名单校验，同时响应浏览器 Private Network Access 预检；不能监听局域网或公网地址，也不能使用任意域名通配。大文件下载必须从临时文件按块流回浏览器，不能把完整 MOV 再一次性读入助手内存。正式向陌生用户分发 macOS 助手前，必须完成 Developer ID 签名与 Apple 公证；开发阶段的临时签名产物不能冒充正式安装包。

## 13. 进度与取消

点击导出后必须立即离开 0%，让用户知道任务已经开始。建议阶段如下：

1. `1%：准备渲染器`
2. 静态自适应边界完成后进入约 `35%`
3. 逐帧渲染与写入占主要区间
4. `加载编码器 / 准备编码器`
5. `编码 ProRes` 或 `组装 PNG 动图`
6. 下载完成后恢复对应按钮

每个导出任务需要独立的取消信号和 UI 状态。取消后应停止后续帧、终止当前编码、删除临时文件并释放离屏页面；不能只把按钮恢复而让后台继续占用 CPU 和内存。

## 14. 错误处理与降级

直接抓帧失败时，可以在记录明确诊断信息后降级到 DOM 截图，但不能静默改变透明度、构图或画质。以下情况应给出具体提示：跨域图片污染画布、字体未加载、WebGL 上下文丢失、输出尺寸超限、内存估算超限、编码器加载失败以及浏览器不支持所需能力。

降级策略也必须保持绝对时间确定性。若兜底路径无法保证与预览一致，应停止并提示，而不是生成一个看似成功但内容错误的文件。

## 15. 自动化验证

每次修改导出内核后，至少完成以下自动检查：

- 类型检查、单元测试、生产构建和 `git diff --check`。
- 对绝对时间、帧数、首尾唯一帧、自适应包围盒和 Alpha 阈值写测试。
- 抽取若干时间点，对比直接抓帧与慢速基准路径的构图、颜色、遮罩、辉光和 Alpha。
- 使用 `ffprobe` 检查 MOV 的 codec、profile、pixel format、帧率、帧数和时长。
- 解码 MOV 中至少一帧，检查透明、半透明和不透明像素是否同时存在。
- APNG 解码完整周期，检查黑色频闪、错误 disposal、空白帧和循环接缝。
- 自适应输出检查四边透明边距，确认没有裁掉辉光，也没有不合理的大面积空白。

自动化通过只代表技术检查通过，不等于用户已经在 Keynote 或目标软件中实际验收。最终仍应由用户检查播放速度、循环接缝、颜色、透明度、边缘质量、构图和文件体积。

## 16. 新应用接入步骤

### 第一步：确定组件类型

确认它属于 Canvas、WebGL、SVG、DOM/CSS、全画幅背景或 3D。找到原始效果的唯一渲染入口，列出所有官方参数、默认值、联动关系和动画周期。

### 第二步：增加绝对时间入口

让组件能够在不依赖实时播放的情况下渲染任意时间点。预览可以继续使用 `requestAnimationFrame`，但它只负责把当前绝对时间传给同一个渲染函数。

### 第三步：实现直接抓帧

优先返回透明 `HTMLCanvasElement`。多层组件在这里完成与预览一致的合成；只有无法直接读取的 DOM/CSS 效果才接入截图兜底。

### 第四步：登记能力

声明抓帧类型、边界策略、是否具有固有背景、支持的输出格式和抓帧入口。以后导出器依据能力工作，不依据案例名称猜测。

### 第五步：接入自适应边界

静态几何使用一次计算；动态几何使用完整周期 Alpha 并集。安全边距必须由真实辉光与阴影范围决定。

### 第六步：复用编码管线

将逐帧透明画布交给统一 MOV/APNG 编码器，不在案例内部复制 ffmpeg 或 APNG 逻辑。

### 第七步：验证预览与导出一致

自动比对关键帧后，再在浏览器和 Keynote 等目标软件中实际检查。明确记录哪些是自动化通过，哪些是用户实际验收。

## 17. 禁止做法

- 不得把预览背景烘焙进透明导出。
- 不得用导出耗时推算动画时间。
- 不得在循环末尾重复第一帧。
- 不得把全部高分辨率解码帧长期留在内存。
- 不得为了统一而把 WebGL 效果改写成 DOM/CSS，或把真实轮廓改成固定圆角矩形。
- 不得用固定大画布冒充自适应导出。
- 不得通过降低透明度、减少辉光、改变颜色或简化动画来伪装性能提升。
- 不得只凭浏览器预览判断 Alpha 和编码正确。
- 不得把“自动检查通过”描述成“用户已经验收”。

## 18. 本项目的参考实现

本项目可从以下文件查看当前实现思路：

- `src/browser-export.ts`：同源离屏渲染、绝对时间抓帧、MOV/APNG 任务、进度、编码与清理。
- `src/adaptive-bounds.ts`：静态边界和 Alpha 包围盒相关计算。
- 各组件自身的导出抓帧入口：用于复用官方渲染、组合多层画布和保持预览/导出一致。

未来若抽成独立包，建议拆分为 `export-core`、`frame-sources`、`adaptive-bounds`、`mov-encoder`、`apng-encoder` 和 `export-validation` 六个模块。应用只负责登记组件能力与实现 `FrameRenderer`，其余能力由公共内核提供。

## 19. 发布前检查清单

- [ ] 原始参数、默认值、联动和动画周期未被改变。
- [ ] 预览与导出使用同一绝对时间状态。
- [ ] 预览背景未进入最终文件，固有内容得到保留。
- [ ] 当前类型使用了最快且忠实的抓帧路径。
- [ ] 自适应范围包含完整周期的主体、阴影和光效。
- [ ] MOV Alpha、帧率、帧数、时长和循环接缝已验证。
- [ ] APNG 无黑色频闪、空白帧和错误循环。
- [ ] 导出任务可取消，完成或失败后资源已清理。
- [ ] 高分辨率与长时长受内存和像素预算保护。
- [ ] 自动化检查结果已记录。
- [ ] 用户实际验收状态已单独记录，未与自动检查混淆。
