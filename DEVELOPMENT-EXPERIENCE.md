# OriginKit → Keynote 开发经验

## 最高原则：忠实接入原始组件

本工具是 OriginKit 与 Keynote 之间的转换器，不是重新设计 OriginKit 的地方。任何组件的画面、动画、参数、默认值、交互和技术路线，都必须以该组件的官方源码和官方在线效果为准。禁止根据截图、名称或视觉印象编写一个“看起来相似”的替代效果；禁止因为现有项目里已经有类似案例，就直接复制旧案例并改名；禁止为了缩短开发时间而删掉官方可调参数、隐藏官方状态或改变参数范围。

接入新组件之前，必须先取得并阅读可核对的官方组件源码及其依赖，确认它究竟使用 CSS Animation、Web Animations API、React 状态、Canvas 2D、WebGL Shader、Three.js 或其他技术。不同类型的案例必须采用与其原始技术相匹配的接入方法，不能把文字动画、鼠标交互、WebGL 背景和真实 3D 对象强行塞进同一种渲染方案。存在多个 Variant 时，应分别核对每个 Variant，不能用一个近似实现代替全部案例。

本项目可以添加的只有外围适配层，包括参数面板、统一时间输入、确定性逐帧渲染、交互录制与回放、Alpha 导出和格式能力声明。外围适配不得改变组件在正常预览状态下的原始行为。没有录制或回放时，Hover、Click、Mouse Move、Scroll 等组件仍应保持官方原本的交互；进行导出时，才由自动化层按照记录的数据重放同样的输入。

官方参数必须完整登记：名称、类型、默认值、最小值、最大值、单位、可选项、条件显示和参数之间的联动关系都要核对。参数控件出现在界面上并不代表接入成功，还必须验证每个控件确实传入原组件、预览中真正生效，并进入逐帧导出的状态数据。预览、单帧导出、MOV/APNG 导出必须共享同一套组件参数和绝对时间；禁止出现预览正确而导出使用另一套近似代码的情况。

导出能力必须按组件真实能力声明。网页视觉效果可以支持透明 MOV 和 APNG；只有能够形成真实三维几何、材质及动画数据的组件才能开放 USDZ。不能可靠转换为真实 3D 的组件必须将 USDZ 按钮置灰并说明原因，不能为了让按钮可用而制作与原作不同的替代模型。

### 每个组件接入前后的检查清单

1. 已取得并阅读官方源码、依赖、Variant 和参数定义，而不是只参考截图。
2. 已确认组件的原始动画技术、时间来源、循环周期和交互触发方式。
3. 官方所有有效参数、默认值、范围、单位、条件显示和联动关系均已登记。
4. 正常预览直接运行官方效果，没有录制时不会被自动化层篡改行为。
5. 参数控件逐项验证为真实联动，而不是仅在界面中显示。
6. 预览与逐帧导出调用同一个渲染器、同一份参数和同一个绝对时间模型。
7. MOV、APNG、USDZ 的按钮状态符合组件的真实导出能力。
8. 已对照官方在线预览检查构图、颜色、光效、位置、速度、循环和交互。
9. 自动化测试与构建通过；实际视觉和 Keynote 表现仍明确交由用户最终验收。

以上清单不是建议，而是新增或修改 OriginKit 组件时必须执行的准入条件。每次继续开发前都应先重新阅读本文件；如果新发现了可复用的失败原因或验证方法，应继续补充到本文件中。

## USDZ 无缝循环：不要重复写入终点姿态

Keynote 会按 USD 舞台声明的 `startTimeCode` 和 `endTimeCode` 播放全部时间码，并在 `playbackMode = "loop"` 时从终点回到起点。它不会自动识别“终点姿态与起点姿态相同”，也不会替导出器删除重复帧。因此，把完整周期的终点再次写入，会造成循环边界多停留一个采样，视觉表现就是规律性卡顿。

正确公式是：`唯一帧数 = round((动画时长 + 开始延迟) × FPS)`，采样范围为 `0...(唯一帧数 - 1)`，`endTimeCode = 唯一帧数 - 1`。例如 3 秒、60 FPS 应写入 180 个唯一采样，时间码为 0～179；不能写成 0～180 的 181 个采样。第 180 帧在数学上等于下一轮的第 0 帧，应该由播放器循环产生，而不是储存在文件里。

无缝性的判断不能用“最后一帧是否等于第一帧”。正确判据是：最后一个唯一姿态到第一帧的角度增量，应与任意两帧之间的正常角度增量相等。本项目在 `src/time.test.ts` 中固定检查硬币翻转和圆环旋转两条运动的循环接缝，防止未来回归。

`playbackMode = "loop"` 只负责告诉支持该元数据的 Apple 播放器循环播放，`autoPlay = true` 只负责自动开始；它们都不会修复错误的帧序列。循环策略、唯一采样数量和首尾运动连续性必须由导出器保证。

如果用户设置了“开始延迟”，每轮循环回到起点后出现这段静止时间是明确的设计结果，不应误判为卡顿。诊断时应先把开始延迟设为 0：只在固定循环接缝停一下，优先检查重复终点帧；在随机位置停顿，才继续调查 Keynote 的实时渲染性能、模型复杂度或设备负载。

每次修改 USDZ 时间轴后，至少完成三层验证：先运行时间模型测试，确认接缝只有一个正常帧步长；再检查生成文件的采样数和 `endTimeCode`；最后运行 Apple `/usr/bin/usdchecker`。这些验证能证明文件结构和时间数学正确，但 Keynote 中的实际播放观感仍应由用户进行最终交互验收。

导入 3D 模型后再导出动画 USDZ 时，还必须检查 Apple 无边记画板内的播放。Three.js 的 `USDZExporter` 默认把可见模型放到 `/Root/Scenes/Scene` 的 `sceneLibrary` 包裹层里；即使动画采样合法、`usdchecker` 通过，并且 Quick Look、Keynote 能播放，无边记仍可能只显示“播放”按钮而不驱动里面的模型。本项目已用用户实际导出的 3D 卡片文件验证：只把 `CoinRing` 等动画节点展开为根节点的直接子树并将根节点标为 `component`，保留原有 `xformOp:orient.timeSamples`、模型、贴图和材质，无边记就能播放。不要为了这个兼容问题重写动画。验收不能只看按钮由“播放”变成“暂停”，必须在画板内实际比较不同时刻的模型姿态；自动化文件检查也不能代替这一项。

Keynote 和无边记的 USDZ 颜色抵消必须分目标处理。几棱体的真机实测档分别对应 Keynote 的 0.5 自发光补光和无边记的 0 补光，均在纸张材质（roughness 0.9、metallic 0）且灯光开启下标定。导出前同一目标档必须覆盖贴图逐像素补偿和纯色补偿；无边记不能沿用旧 117 色档或 Keynote 的补光，否则校准条件失配。偏色抵消开关只控制是否启用补偿，不改变两套目标导出入口；取消勾选时保留原图颜色。

Coin Loader 的可选“扇面展开”属于外层编排，不改导入模型的几何、材质或内部动画。完整周期先把各模型的左下角对准圆环的同一个中心点，像一叠扑克牌那样绕该点依次转开，每张相隔 360°/数量；铰链转开时圆环整体尚不旋转。每张展开后的中心射线必须与其原有编号槽位的射线重合，然后沿这条射线径向扩散到槽位，不能在扩散时转向、交叉或重新排列。从开始沿半径移动起圆环整体旋转；每张模型须等径向展开达到实际移动距离的 75%、彼此已有足够间距时才以缓入曲线启动自身翻转，到位后继续翻转。几何收拢按扩散、铰链转开的逆序进行；圆环持续旋转，但单张翻转在径向收回到 75% 间距前缓出并完成完整圈数，之后停止自身翻转，避免近距离卡片互相穿插。不能以模型中心沿圆周平移来冒充铰链展开，也不能用两端重叠的 0°/360° 位置占掉一张模型。展开和收拢只调度原模型位置与朝向，首尾姿态连续，导出只写唯一帧。位置和朝向的取样须由预览和 USDZ 导出共用同一时间求值器；MOV/PNG 的离屏渲染接收同一组参数。

Coin Loader 导出动画 USDZ 时，起始帧是一束重叠的牌，不能让使用方只按这一帧的几何范围决定取景。导出器应按全部唯一帧的真实三维变换累计卡片外包络，并在根模型写入留有边距的 `extentsHint`；横纵取较大跨度形成方形安全范围，使 Keynote、无边记等方形 3D 视口能容纳展开后的完整圆环。该范围只用于模型取景，不得缩放几何或改变预览和动画采样。

## Vercel 网页导出：复用 3D 项目的浏览器编码链

部署后的 Vite 页面不能调用本机 Node、Playwright 或系统 FFmpeg，所以 `/api/export` 只适合本地服务，不能作为线上导出方案。本项目应复用透明 3D 展台已经验证的浏览器链路：同源离屏页面按绝对时间逐帧渲染，Canvas 直接生成透明 PNG Blob，再由随站点发布的单线程 `ffmpeg.wasm` 编码 APNG 或 ProRes 4444 MOV。首轮使用需要加载约 32 MB 的编码器，整个过程中页面必须保持开启。

浏览器导出仍然是确定性逐帧渲染，不是录屏。高分辨率长动画不得先把全部 PNG Blob 和解码位图同时留在 JavaScript 内存中：普通画布必须逐帧渲染并立即交给目标编码器；自适应画布使用两遍确定性渲染，第一遍只累计完整周期的可见 Alpha 并集，第二遍逐帧裁切并立即编码。不得再用固定的 192 MB 累计帧缓存线截断本来有效的导出，但继续保留 8192 像素单边上限、3600 帧上限和 20 亿总像素上限；MOV 的 WebAssembly 文件系统与最终 APNG 文件自身所需的编码内存不属于可消除的源帧重复缓存。MOV 编码使用 `prores_ks`、4444 XQ、`yuva444p10le` 和 Alpha，并把容器里的 FFmpeg vendor 标记修正为 Apple `apl0`。验收时必须用 `ffprobe` 检查编码、帧率、帧数与时长，再解码一帧确认透明、半透明和不透明像素同时存在。

MOV 的中间帧由 Canvas `toBlob("image/png")` 直接生成，本身已经是无损 PNG。禁止为了缩小这些只在 WebAssembly 文件系统中短暂存在的中间文件，再对每一帧分别启动一次 `ffmpeg.wasm` 最高等级 PNG 重压缩；这种处理不会改善最终 ProRes 4444 XQ 的画质或透明通道，只会把一次视频编码变成数百次单帧编码。中间 PNG 应逐帧直接写入编码器文件系统，全部帧准备完成后只执行一次 ProRes 编码。

自适应导出不等于所有案例都必须先完整渲染一遍。颜色只沿静止闭合轮廓运动的边框类案例，其完整周期外包络由背景插图的可见 Alpha 边界、面板变换、描边包裹位置和最大光晕半径唯一决定；没有动画背景 PNG 和额外 PNG 图层时，应直接用这些几何量求出保守安全裁切，跳过第一遍逐帧扫描。背景 PNG 是动图、存在额外可移动图层，或组件真实几何会随时间改变时，继续使用完整周期 Alpha 并集，不能为了提速冒险裁掉动态内容。

Canvas/WebGL 驱动的组件不得默认通过 `html-to-image` 每帧克隆整棵 DOM。能够取得原组件 Canvas、同一背景插图 Canvas 和同一 Alpha 遮罩时，应在逐帧时间更新后直接把这些既有像素按预览中的实际矩形、遮罩、混合与光晕参数合成到透明输出 Canvas；这属于同一渲染器的无损读取，不是另写近似动画。`html-to-image` 只作为尚未提供直接像素出口的 DOM/CSS 组件回退方案。直接合成通道必须与慢速回退抽帧对照，确认构图、透明度、遮罩和光效一致后才能扩展到更多组件。

## 组件扩展：注册原组件，不复制整页

每个 OriginKit 组件通过 Component Registry 声明名称、分类、原始渲染器、触发方式和支持的导出格式。页面只负责选择注册项并展示其参数，组件自身负责按绝对时间渲染。新增组件必须使用可核对的 OriginKit 官方源码；只拿文档参数重新模仿一个相似动画，不能算引入原组件。网页效果不一定都能转换为 USDZ，因此 MOV、APNG 与 USDZ 能力需要逐组件显式声明。
