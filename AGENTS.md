# 前端→Keynote 项目开发入口

开始本项目的任何开发、修复、重构、组件接入或导出工作之前，必须先完整读取根目录的 `DEVELOPMENT-EXPERIENCE.md`，并把其中的“最高原则：忠实接入原始组件”作为组件工作的第一判定标准。

不得凭截图或视觉印象仿写 OriginKit 效果，不得为了复用现有实现而强行把不同技术类型的组件改造成同一种方案，也不得擅自删减官方参数、默认值、联动关系或交互状态。需要修改官方组件外围代码时，只能增加本项目所需的时间控制、参数桥接、交互录制和导出适配，不能改变原始效果本身。

每次组件工作结束前，必须按照 `DEVELOPMENT-EXPERIENCE.md` 的组件接入检查清单逐项核对，并清楚区分“自动化检查通过”和“用户已经实际验收”。
